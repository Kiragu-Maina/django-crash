{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "apt update && apt install -y npm && npm install yuglify && python manage.py makemigrations && python manage.py migrate && python manage.py collectstatic --noinput && python manage.py populate_users && daphne -b 0.0.0.0 -p $PORT crashsite.asgi:application",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
